# Patch registry

Every patch we carry or intend to send gets a row here.

Status moves `draft` -> `ready` -> `sent` -> `merged` / `rejected`. A row is
`ready` only when its directory under `docs/upstream/` is complete and the
checklist in `CONTRIBUTING-UPSTREAM.md` section 9 passes. `sent` is set by
whoever sends it, which is always May.

| Package | Patch / change | What it does | Upstream | Where | Status |
|---|---|---|---|---|---|
| nux | `migrate-to-libpcre2.patch` (0ubuntu13) | Not ours. Adds the `nux.pc.in` and `configure.ac` hunks the 0ubuntu12 upload left out, so `nux-4.0` resolves again and unity can build. Asking for an SRU into resolute. | LP: #2103918, LP: #2147013 | [`upstream/nux-pcre2/`](upstream/nux-pcre2/) | **ready** |
| light-locker | `0003-Follow-the-user-s-display-session-outside-a-session-.patch`, `0004-Find-the-LightDM-session-when-XDG_SESSION_PATH-is-mi.patch` | Stops the abort on every login under Ubuntu Unity 26.04. light-locker runs under cinnamon-session, a systemd user service, so logind cannot map its PID to a session and `XDG_SESSION_PATH` is not inherited. Falls back to logind's display session and to LightDM's session list over D-Bus. Ours; 1.8.0-3ubuntu4+unity2. | LP: #2038808 | [`upstream/light-locker-session/`](upstream/light-locker-session/) | **ready** |
| unity | `GnomeSessionManager: only the session manager confirms a pending action` (commit on `unity/resolute`; native package, no quilt) | Unity waits for the session manager to call `EndSessionDialog.Open` back; cinnamon-session never does. After its dialog is cancelled the stale action either kills the session menu (known issue #2) or, if it was a restart, makes the next menu click restart the machine with no dialog. Only the owner of `org.gnome.SessionManager` may now confirm. Ours; 7.7.1+26.04.20260306-0ubuntu3+unity2 (`+unity1` fixed #2 only). Two unit tests. | none yet (MR to gitlab ubuntu-unity/unity, `ubuntu/devel`) | [`upstream/unity-stale-pending-action/`](upstream/unity-stale-pending-action/) | **ready** |
| unity | `GnomeSessionManager: show inhibitors before confirming a pending action`, `SessionController: bring a fading view back when asked to show again` (branch `wip/confirm-inhibitors`) | A restart or shutdown from the session menu went past applications that inhibit it: Unity confirmed its own pending action even when the session manager's `Open` listed inhibitors, and a new dialog requested while the old one faded out was ignored. Ours; 7.7.1+26.04.20260306-0ubuntu3+unity4 (`+unity3` had the first fix only, never published), in aptly. | none yet | `research/shutdown-path/` | local |
| cinnamon-session | `Ask-the-shell-for-the-end-session-dialog-when-Cinnamon-is.patch`, `Don-t-ask-a-Cinnamon-that-is-not-running-to-close-its-dia.patch` | Option A for known issue #6: when Cinnamon is absent, ask `org.gnome.Shell` for the end-session dialog after the query phase, as gnome-session does; stop two CRITICALs per cancelled end session. Ours; 6.4.2-1+unity1, in aptly together with the compiz fix it needs. | none yet (feature request to linuxmint/cinnamon-session). "Don't ask..." has an upstream equivalent in master cbcc364 (not in 6.6.4); drop it on a rebase to master, do not fuzz it (release-recheck-a) | `research/shutdown-path/` | local |
| unity | `GnomeSessionManager: don't go around a session manager that said no` | Logout/Reboot/Shutdown fell back to logind on any error, including `NotInRunning` (session already ending, waiting on inhibitors) and `LockedDown` (policy): the machine restarted past inhibitors and lockdown. Ours; 7.7.1+26.04.20260306-0ubuntu3+unity5, in aptly. | none yet | `research/shutdown-path/` | local |
| cinnamon-session | `Ask-for-a-dialog-when-inhibitors-have-nobody-to-be-shown-to.patch` | A logout without confirmation (Unity's) hung in the query phase when inhibited, with no dialog to show the inhibitors. Ours; 6.4.2-1+unity2, in aptly. | none yet | `research/shutdown-path/` | local |
| compiz | XSMP die callback calls `exit(0)` with threads running | Crashes in the GDBus worker at logout when the bus is busy; frequent under #6 option A (3 in 7 restarts). `_exit(0)` after `CompSession::close()`: 0 in 13. Ours; 1:0.9.14.2+25.10.20250930-0ubuntu3+unity1, in aptly, https://github.com/Ubuntu-Unity-LifeSupport/compiz. | none yet | `research/shutdown-path/` | local |
| unity-settings-daemon | `cursor: don't hide the pointer when the plugin starts`, `debian: run the test suite under xvfb-run` (branch `unity/resolute` from `ubuntu/devel`) | Candidate fix for known issue #1 (+unity1..+unity3), read from code. **Cursor change reverted in +unity4** - the measured cause is the idle monitor (row below); the xvfb test change stays. | LP: #1390628 (related) | `research/cursor-after-login/` | reverted |
| unity | `tests: build with googletest 1.17 and GCC 15` | `tests/` switches to C++17 (googletest 1.17); `gmockvolume.c` passes `G_ASYNC_RESULT` instead of `G_TASK` (GCC 15). A clean tree now builds `test-gnome-session-manager` (51) and `test-session-controller` (20), both pass. Tests stay off in the package build; running them there still needs nux without the VidMode crash (agent B). On `unity/resolute` after `+unity5`, goes into the next build. | none yet | DECISIONS 2026-09-23 | local |
| nux | `fix-missing-vidmode.patch` (0ubuntu13+unity1, carried to 0ubuntu15+unity1) | `CreateOpenGLWindow` dereferenced an unchecked XF86VidMode mode list (`GraphicsDisplayX11.cpp:297`): any standalone Nux window segfaulted on Xvfb and Xvnc, which kept Unity's tests off Xvfb. Unity itself is not affected. Ours; verified on target2; Unity's unit tests now pass under plain Xvfb (51/51, 20/20, agent A). In aptly. | none (upstream on hold) | [`research/nux-vidmode/`](research/nux-vidmode/) | draft |
| unity | `ThumbnailGenerator: don't join threads that were never started`, `unitya11y: don't remove from the accessible table after finalize` | compiz segfaulted in `~UnityScreen` on every exit (SIGHUP restart, `systemctl` stop): `pthread_join()` on a thread never created. Plus the generator's other thread bugs and 421 `g_hash_table` criticals per exit. Upstream's own `TestNoURIThumbnail` crashed; passes now. Ours; unity `+unity8` (first in unpublished `+unity6`), in aptly. | none yet | [`research/compiz-restart/`](research/compiz-restart/) | local |
| unity | `DecoratedWindow: redraw when the decoration elements change`, `DecoratedWindow: draw the decoration of a window without a shadow` | Yaru sets the inactive shadow radius to 0; Unity then drew nothing of a window's decoration until it had been focused once. After a compiz restart every window but one had no title bar or buttons. Ours; unity `+unity8`, in aptly. | none yet | [`research/compiz-restart/`](research/compiz-restart/) | local |
| compiz | `window: keep the frame-extents clamp on the window's own viewport` | The first-decoration workarea clamp in `setWindowFrameExtents` used the current viewport's workarea, so every compiz restart moved windows on lower workspaces up by a title bar until they landed on the first row - the release notes' side bug of the #3 workaround. Ours; 1:0.9.14.2+25.10.20250930-0ubuntu3+unity2, in aptly. | none yet | [`research/compiz-restart/`](research/compiz-restart/) | local |
| gtk-nocsd | `crash-handler-realign-stack.patch`, `crash-handler-initialise-arguments.patch` | Backports of upstream 664d8c6 and d851645 (in 4.0): the crash handler segfaulted at every crash of a preloaded program on glibc 2.43 instead of restarting it, compiz included. Not ours; 0~20260321+0b77e1b-1+unity2, in aptly, https://github.com/Ubuntu-Unity-LifeSupport/gtk-nocsd. | upstream fixed | [`research/compiz-restart/`](research/compiz-restart/) | dropped in 4.8-1+unity1 (part of 4.0) |
| gtk-nocsd | Merge of Debian 4.8-1 (no patches of ours) | Rebase from the 2026-03-21 snapshot to 4.8, the version in Debian and 26.10. Fixes Chromium browsers losing their window buttons under Unity (upstream ecd66fe, codeberg #67, [LP #2158965](https://bugs.launchpad.net/bugs/2158965)) and a gnome-sound-recorder crash. 4.8-1+unity1, in aptly, https://github.com/Ubuntu-Unity-LifeSupport/gtk-nocsd. | upstream release; resolute needs an SRU | [`research/gtk-nocsd-4.8/`](research/gtk-nocsd-4.8/) | local |
| unity-session | `run-systemd-session: don't stop graphical-session.target when it is not running` | At every login the script stopped graphical-session.target to clear leftovers; stopping it while inactive still killed gvfs-daemon when ibus-daemon was activating it that millisecond, and dbus-daemon kept the activation pending for 120 s: black desktop for two minutes, 3 logins in 13 on target. Now stops only when a target is not inactive. Ours; 49.4+unity1, in aptly, https://github.com/Ubuntu-Unity-LifeSupport/unity-session. | none yet | [`research/login-gvfs-race/`](research/login-gvfs-race/) | local |
| unity | `FileManager: fall back to Nemo for an unknown default file manager`, `DecoratedWindow: rebuild the shaped shadow when its pixmap is gone` | Two third-party crash reports with patches, reproduced and fixed: compiz crashed at every start when the default file manager was neither Nautilus nor Nemo (LP #2160299, patch by Riku); shaped override-redirect windows with an empty shape in between (Wine menus) crashed compiz (LP #2165662, patch by OldSamuray). Ours to carry; unity `+unity9`, in aptly. | LP: #2160299 (MP 508187, needs review), LP: #2165662 | [`research/unity-lp-crashes/`](research/unity-lp-crashes/) | local |
| unity-settings-daemon | `color: disconnect from the session proxy when the plugin stops` | The color plugin left its handler on the session manager proxy shared by all plugins; after stop/finalize a property change - the session manager leaving the bus at logout - called into a stopped, then freed manager: SIGSEGV in `cd_client_get_connected`. Disconnect in stop, `g_signal_connect_object` as in gnome-settings-daemon fb2ca61f. Plus `color: create the plugin's resources in start(), not in init()` - switched off and on, the plugin stayed dead. Ours; 0ubuntu7+unity3, in aptly. | none | [`research/usd-color-logout-crash/`](research/usd-color-logout-crash/) | local |
| lightdm | `session-child: leave the SIGTERM handler with _exit(), not exit()` (quilt, `0009-...`) | Third-party report: `exit()` from the SIGTERM handler could deadlock in `free()` under the malloc lock and hold the next session for systemd's 90 s stop timeout. Not triggered by target's default PAM stack; reproduced on the real binary with the reporter's conditions (libgnutls loaded), fixed. Ours to carry; 1.32.0-6ubuntu4+unity1, in aptly, https://github.com/Ubuntu-Unity-LifeSupport/lightdm. | LP: #2168421, canonical/lightdm#484 | [`research/lightdm-sigterm-exit/`](research/lightdm-sigterm-exit/) | local |
| cinnamon-session | backport of upstream 9409c18b (`csm-systemd-wait-for-logind-s-PrepareForShutdown-before-q.patch`), `Request-the-reboot-or-shutdown-only-once.patch` | Upstream #214: quit once logind prepares for shutdown instead of lingering while a delay inhibitor holds the restart. Ours on top: under Unity `csm_manager_quit()` ran 12 times and logind refused the repeats. Measured: one request, session gone in 3 s. 6.4.2-1+unity3, in aptly. | linuxmint/cinnamon-session#214 (9409c18 in master 6.7-unstable only, not in 6.6.4; quit-once still needed on master - release-recheck-a) | [`research/cinnamon-session-214-202/`](research/cinnamon-session-214-202/) | local |
| nux | `Validator::Validate` discards its match result | On the Windows branch the function returns `Acceptable` from both sides of its `if`. Real, small, and deliberately kept out of the PCRE2 submission so it does not blur a first contact. | none yet | — | draft |

## Rules

- Patches live as quilt series in `debian/patches/`, managed with `gbp pq`.
  Exception: a `3.0 (native)` package such as `unity` has no patch series -
  changes are commits on `unity/resolute`, and the upstream copy is a
  separate branch cut from upstream's branch (see DECISIONS 2026-09-23).
- A patched Ubuntu package gets a `+unity1` version suffix. A package we build
  unmodified from upstream git keeps the upstream version, as `nux` 0ubuntu13
  does - we are not the author and should not claim to be.
- When Ubuntu ships a new version of a patched package, rebase the series onto
  it rather than carrying a fork.
- Anything that could plausibly land upstream goes upstream first. A `local`
  row sitting for a long time is a smell.
- Findings made in passing become their own row, never a passenger on someone
  else's patch.
| calamares-settings-ubuntu | `basicwallpaper: desktop window on X11, so it cannot cover Calamares` (commit on `unity/resolute`; native package) | Known issue #4. In the OEM first-time setup session (xfwm4 + basicwallpaper + Calamares) the wallpaper was a fullscreen window; xfwm4 raises a focused fullscreen window above all, so it covered Calamares when it mapped after it (a startup race) or on Alt+Tab. Now a `_NET_WM_WINDOW_TYPE_DESKTOP` window that takes no focus, X11 only (Kubuntu's Wayland path unchanged). Ours; 1:26.04.12+unity1, in aptly; verified in Xvfb on target2, in a chroot and on a real two-stage OEM install from the official ISO (VM `oem-test`), where the archive binary hid Calamares on 3 of 3 cold first boots and ours on 0 of 2 (snapshots `OEM-ready`, `OEM-ready-fixed`). | none (upstream on hold) | [`research/calamares-oem/`](research/calamares-oem/) | local |
| indicator-bluetooth, indicator-printers | `Build-depend on systemd-dev` (debian/control; branch `unity/resolute`) | The 26.04 rebuild dropped `/usr/lib/systemd/user/*.service` (configure takes the unit dir from `systemd.pc`, which moved to `systemd-dev`), so neither indicator service ever started. Ours; 0ubuntu7+unity1 / 0ubuntu8+unity1, in aptly; verified on target2 (units active, services running). | LP: #2153375 (bluetooth; printers not reported) | [`research/indicator-units/`](research/indicator-units/) | local |
| nux | `fix-fbo-attachment-arrays.patch` (0ubuntu15+unity2) | `FormatFrameBufferObject()` cleared the FBO attachment vectors right after they were sized, so every attachment call indexed an empty vector: aborts under `-D_GLIBCXX_ASSERTIONS` (the reporter's GCC 16 crash) and leaks the last attached texture (measured on +unity1). Reset the elements instead. Ours; in aptly; Unity's tests pass against it (agent A). | LP: #2160298 (reporter's MR 508190, unreviewed) | [`research/nux-fbo/`](research/nux-fbo/) | local |
| appmenu-gtk-module | `2001_make-module-resident.patch` (upstream a783b01c) | GTK3 unloads a gtk-module loaded through the `gtk-modules` setting when it changes; this one hijacks GtkMenuBar, so the app segfaults. Not reachable in our default session (module comes from `GTK_MODULES`, never unloaded), reproduced with the setting path. Upstream's fix, not yet released; 25.04-1build1+unity1, in aptly. | LP: #2166410 | [`research/appmenu-resident/`](research/appmenu-resident/) | local (upstream) |
| indicator-datetime, -power, -session, -sound, -keyboard | +unity1 build fixes (branch `unity/resolute`; datetime `7676fbe`, power `bff6e5d`, session `47aac89`, sound `338d8fd` cherry-picked from 26.10, keyboard `ad9d0bf`) | None of the five could be built in resolute: CMake 4 minimum version, GCC 15 pointer casts, libnotify 0.8 icon hint in datetime's tests, `lightdm-vala`/`systemd-dev` for keyboard. No behaviour change; file lists match the archive. Needed before any of them can take a real fix. `research/indicator-ftbfs/` | Ours; sound's is 26.10's (LP #2166355). datetime/power/session unfixed in 26.10, upstream dead |
| indicator-keyboard | +unity2: `tests/main.vala` emits the mock's notify with `notify_property()`; `debian/rules` makes tests fatal again (branch `unity/resolute`, `cb8cf96`, `1a14712`) | Vala >= 0.55.1 compiles `notify["command"] (pspec)` to `g_object_notify (self, pspec)`, so the test run aborted at test 2 and tests 3-9 never ran since 2022 (LP #1968333, hidden by `|| true`). Test-only; all 9 pass. `research/indicator-ftbfs/` | Ours. Vala codegen bug not reported; LP #1968333 fix not sent (May) |
| xorg-server | `debian/patches/upstream-21.1.24/` - the 31 upstream commits 21.1.22..21.1.24, plus `upstream-21.1-branch/` 8d604fa14 (FindGlyphRef use-after-free, LP #2163497), on Ubuntu's 1ubuntu1.3 (`2:21.1.22-1ubuntu1.3+unity2`) | Closes 11 CVEs open in resolute (CVE-2026-50256..50264, 55999, 56000) and the FindGlyphRef crash. Not ours. Versioned so any later Ubuntu upload supersedes it; rebase on each (xorg-watch timer) with `research/xorg-versioning/patches/series-carried.txt`. Verified on target, in aptly. | upstream releases / server-21.1-branch; Ubuntu security has the CVEs needs-triage | [`research/xorg-versioning/`](research/xorg-versioning/) | local (upstream) |
| libindicator | `Build-depend on systemd-dev` (debian/control; branch `unity/resolute`, `f75a617`) | FTBFS in resolute: `indicators-pre.target` not installed (`systemd.pc` moved to `systemd-dev`, never a declared build-dep), `dh_install --fail-missing` stops. The target must ship: `unity-panel-service` has `BindsTo=indicators-pre.target`. Ours; 0ubuntu8+unity1 built, file lists and exported symbols equal the archive's; **not in aptly** (archive binary equivalent, goes in with the first real change). | none (26.10 same version, upstream dead) | [`research/rebuild-trial/`](research/rebuild-trial/) | local |
| gtk-nocsd | `debian/51gtk-nocsd` -> `/etc/X11/Xsession.d/51gtk-nocsd` (branch `unity/resolute`, `a492994`) | X sessions started from Xsession without the systemd user manager (Xfce, MATE, LXQt, window managers) did not load gtk-nocsd: 4.8-1 sets `LD_PRELOAD` only in `environment.d` and removed the gtk3-nocsd Xsession scripts. Adds the library unless present (no duplicate under Unity) and passes it past a setgid ssh-agent. 4.8-1+unity2, in aptly, https://github.com/Ubuntu-Unity-LifeSupport/gtk-nocsd. | Debian packaging; no Debian/LP/Codeberg report found; systemd#7641 open | [`research/nocsd-gaps/`](research/nocsd-gaps/) | local |
| unity-settings-daemon | `idle-monitor: keep the X event filter when the D-Bus name is lost`, `Start the daemon once per session: from the systemd user unit`, `debian: install the helpers where their callers look for them` (branch `unity/resolute`, `90f5773`, `85d3511`, `b503ffd`) | Known issue #1: the daemon started twice, raced for org.gnome.Mutter.IdleMonitor, and losing the name removed the X event filter of every idle watch - the cursor plugin never showed the pointer it hid at login (archive: 2 of 12 logins; ours: 0 of 12). Plus libexecdir back to /usr/lib/unity-settings-daemon (autostart, localeexec, backlight/Wacom polkit pointed at /usr/libexec since the 0ubuntu7 base). Ours; 0ubuntu7+unity4, in aptly, https://github.com/Ubuntu-Unity-LifeSupport/unity-settings-daemon. | gitlab ubuntu-unity/issue-tracker #161; LP #1390628 (old, same symptom) | [`research/cursor-after-login/`](research/cursor-after-login/) | local |
| unity | `DecorationsEdge: don't start a move or resize while one is running` (commit on `unity/resolute`, `f2268bef`) | Known issue #3: a second button pressed on a window edge during a border drag went through Edge::ButtonDownEvent again - XUngrabPointer took the pointer grab from compiz's running resize, whose "resize" grab then stayed in compiz; with a compiz grab held, compiz stops thawing the synchronous button grabs of window frames and the next click froze the pointer (moves, clicks nothing, until compiz restarts). Before: 16 of 50 border-drag variants stuck; after: 0 of 50, resizing intact. Ours; unity `+unity10`, in aptly, https://github.com/Ubuntu-Unity-LifeSupport/unity. | gitlab ubuntu-unity/issue-tracker #165; LP: #1885435, #1644412 | [`research/cursor-stops/`](research/cursor-stops/) | local |
| indicator-keyboard | +unity3: `lib/input-sources.vala` (NULL InputSources is no layouts), NULL user name not passed to LightDM, test `xkb-input-sources` (branch `unity/resolute`, `10eb95c`) | The greeter's service crashed in `g_variant_iter_new` when AccountsService had no InputSources cached (accounts-daemon restart). LP: #2166139 | not fixed in 26.10 (0ubuntu4) | [`research/indicator-keyboard-2166139/`](research/indicator-keyboard-2166139/) | local |
| indicator-datetime | +unity2: `src/engine-eds.cpp` places a VTODO without DTSTART at its DUE, skips components without a time; test `test-eds-ics-tasks-without-start` (`846dfa0`) | A task with only a due date (Nextcloud Tasks) aborted the service in `DateTime::get()`. LP: #1848969, #2099742 | not fixed in 26.10; Ayatana 47e005d only removes the assert | [`research/indicator-datetime-tasks/`](research/indicator-datetime-tasks/) | local |
| libunity | +unity1: `debian/patches/python-3.12-no-imp.patch` - scope-runner-dbus.py loads source scopes with importlib (`78c98ec`) | unity-scopes-runner imported `imp` (removed in Python 3.12) and failed at once on 3.14, so no Python scope ran | not fixed in Debian -8 / 26.10 | [`research/libunity-python314/`](research/libunity-python314/) | local |
| unity-scope-{calculator,devhelp,gnote,manpages,tomboy,virtualbox,zotero} | +unity1: `debian/rules` drops `/usr/lib/python3/dist-packages/__init__.py`; manpages: `gi.require_version('Gtk', '3.0')`; gnote: retry while Gnote registers RemoteControl (debdiffs in research) | The ten Python scopes could not be installed together (same file); manpages failed every search on GTK 4; gnote found nothing unless Gnote was already running | none upstream (Ubuntu-native, same in 26.10) | [`research/unity-scopes/`](research/unity-scopes/) | local |
| unity-greeter | `Build-depend on liblightdm-gobject-dev and lightdm-vala`, `tests: take Gdk.Key values as uint`, `debian/rules: run the tests without make parallelism` (branch `unity/resolute`) | Could not be rebuilt in resolute: lightdm's split left only an unversioned Provides for `liblightdm-gobject-1-dev` and moved the vapi to `lightdm-vala`. Test program compiles and runs again (tests still ignored as before). Ours; 25.04.1-0ubuntu1+unity1, in aptly, https://github.com/Ubuntu-Unity-LifeSupport/unity-greeter. Verified live: greeter, password login, user switch and back. | none (26.10 has the same unrebuilt source) | [`research/unity-greeter-rebuild/`](research/unity-greeter-rebuild/) | local |
| indicator-messages | 0ubuntu8~26.04.1: no-change backport of 26.10 0ubuntu8 (systemd-dev in Build-Depends) | the 0ubuntu7 rebuild failed in resolute (the user unit would be lost). LP: #2166912 | fixed in 26.10 (0ubuntu8), carried as a backport | [`research/rebuild-loss/`](research/rebuild-loss/) | local |
