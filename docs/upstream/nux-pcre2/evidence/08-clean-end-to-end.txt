# End-to-end verification on a clean, fully updated Ubuntu Unity 26.04
#
# Target rolled back to snapshot Clean-updated-2026-09-23, then the package
# installed from a local archive with a single apt command - the path a user
# actually takes. Every earlier install in this project went through scp and
# dpkg -i, which tests the package but not the delivery path.

host:    mike-virtualbox, Ubuntu 26.04.1 LTS
pending: 4 packages (phased updates)

before, as recorded during the run - the stock version from resolute/universe:

  libnux-4.0-0        4.0.8+18.10.20180623-0ubuntu12
  libnux-4.0-common   4.0.8+18.10.20180623-0ubuntu12

installed with:

  sudo apt-get install --only-upgrade libnux-4.0-0 libnux-4.0-common

apt fetched them over the host-only network:

  Get:1 http://192.168.56.10:8080 resolute/main amd64 libnux-4.0-0 amd64 4.0.8+18.10.20180623-0ubuntu13 [783 kB]
  Get:2 http://192.168.56.10:8080 resolute/main amd64 libnux-4.0-common all 4.0.8+18.10.20180623-0ubuntu13 [52.2 kB]
  Get:3 http://192.168.56.10:8080 resolute/main amd64 nux-tools amd64 4.0.8+18.10.20180623-0ubuntu13 [10.5 kB]

after:
  libnux-4.0-0:
    Installed: 4.0.8+18.10.20180623-0ubuntu13
    Candidate: 4.0.8+18.10.20180623-0ubuntu13
    Version table:
   *** 4.0.8+18.10.20180623-0ubuntu13 500
          500 http://192.168.56.10:8080 resolute/main amd64 Packages

The machine was then rebooted, and the session came up on its own via
autologin. Shared objects mapped by the running compiz:
  libnux-4.0.so.0.8.0
  libnux-core-4.0.so.0.8.0
  libnux-graphics-4.0.so.0.8.0
  libpcre2-8.so.0.14.0

No PCRE1 mapping is present.

UI checklist, driven with xdotool: desktop, Dash on Super, HUD on Alt, session
indicator and sound indicator all render and respond. Screenshot in
docs/screenshots/2026-09-23-verification-clean-target.png.

Crashes during the run: light-locker only. That is a documented 26.04 known
issue ("light-locker seems to crash on login but login works") and it is still
present after all pending updates were applied, so it is unrelated to this
package.
